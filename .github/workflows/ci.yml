name: CI

on:
  push:
    branches: [ "arena/019f6683-meow-meow-repo", "main" ]
  pull_request:
    branches: [ "arena/019f6683-meow-meow-repo", "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run a successful test
        run: |
          echo "Running tests..."
          exit 0
